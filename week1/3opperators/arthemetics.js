let a = 21  , b = 6, sum, difference, product, quetient, remainder;

sum = a + b;

product = a * b;

difference = a - b;
quetient = a / b;   // the division operator

remainder = a % b;  // The remainder operators

a++   // This is the increament operator
b--  // This the  decreament operator

// you can console the above results as you need

console.log(`sum:${sum} , a : ${a}, remainder:${remainder},  b:${b}
difference:${difference}, product:${product} ` )
